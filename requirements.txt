# =============================================================================
# Content Intelligence System — Dependencies
# =============================================================================
# Core framework
fastapi==0.115.12
uvicorn[standard]==0.34.2

# Data validation (ships with FastAPI, pinned for stability)
pydantic==2.11.1
pydantic-settings==2.9.1

# Environment variable management
python-dotenv==1.1.0

# OpenAI API client
openai==1.78.1

# Async HTTP client (used by OpenAI SDK internally)
httpx==0.28.1

# CORS middleware is built into FastAPI — no extra dep needed
